export default function Home() {
  return (
    <main style={{ padding: 24, fontFamily: "system-ui, sans-serif" }}>
      <h1>Rider → Advance Form</h1>
      <p>If you can see this, the build is working.</p>
      <p>Now visit <code>/</code> (this page) and also try <code>/api/extract</code> with POST from the UI in <code>app/page.tsx</code>.</p>
    </main>
  );
}
